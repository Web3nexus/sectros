<IfModule mod_rewrite.c>
    RewriteEngine On

    # Only rewrite if not already going to backend/public
    RewriteCond %{REQUEST_URI} !^/backend/public/
    RewriteRule ^(.*)$ backend/public/$1 [L]
</IfModule>
